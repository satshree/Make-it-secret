import React from 'react'

export default function Decrypt() {
    return (
        <React.Fragment>
            <button className="btn btn-success btn-lg"> Decrypt </button>
        </React.Fragment>
    )
}